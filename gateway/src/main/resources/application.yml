spring:
    application:
        name: gateway

feign:
  client:
    config:
      feignName:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full

book-service:
    ribbon:
        eureka:
            enabled: false
        listOfServers: localhost:8081, localhost:8082
        ServerListRefreshInterval: 15000
